/* ============================================================ */
/* Stream == 1 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_1_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_1_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_1_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_1_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_1_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_1_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_1_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_1_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_1_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_1_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_1_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_1_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_1_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_1_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_1_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_1_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_1_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_1_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_1_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_1_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_1_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_1_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_1_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_1_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_1_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_1_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_1_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_1_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_1_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_1_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_1_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_1_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 64 */
__global__ void intensity_kernel_1_64(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 128 */
__global__ void intensity_kernel_1_128(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 256 */
__global__ void intensity_kernel_1_256(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 512 */
__global__ void intensity_kernel_1_512(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 2 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_2_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_2_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_2_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_2_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_2_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_2_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_2_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_2_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_2_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_2_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_2_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_2_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_2_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_2_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_2_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_2_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_2_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_2_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_2_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_2_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_2_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_2_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_2_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_2_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_2_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_2_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_2_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_2_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_2_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_2_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_2_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_2_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 64 */
__global__ void intensity_kernel_2_64(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 128 */
__global__ void intensity_kernel_2_128(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 256 */
__global__ void intensity_kernel_2_256(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 512 */
__global__ void intensity_kernel_2_512(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 1024 */
__global__ void intensity_kernel_2_1024(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2048 */
__global__ void intensity_kernel_2_2048(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4096 */
__global__ void intensity_kernel_2_4096(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 4 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_4_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_4_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_4_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_4_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_4_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_4_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_4_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_4_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_4_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_4_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_4_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_4_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_4_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_4_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_4_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_4_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_4_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_4_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_4_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_4_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_4_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_4_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_4_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_4_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_4_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_4_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_4_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_4_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_4_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_4_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_4_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_4_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 8 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_8_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_8_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_8_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_8_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_8_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_8_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_8_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_8_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_8_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_8_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_8_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_8_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_8_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_8_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_8_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_8_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_8_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_8_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_8_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_8_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_8_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_8_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_8_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_8_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_8_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_8_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_8_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_8_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_8_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_8_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_8_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_8_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 16 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_16_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_16_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_16_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_16_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_16_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_16_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_16_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_16_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_16_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_16_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_16_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_16_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_16_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_16_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_16_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_16_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_16_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_16_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_16_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_16_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_16_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_16_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_16_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_16_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_16_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_16_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_16_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_16_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_16_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_16_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_16_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_16_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 32 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_32_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_32_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_32_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_32_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_32_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_32_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_32_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_32_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_32_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_32_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_32_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_32_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_32_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_32_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_32_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_32_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_32_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_32_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_32_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_32_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_32_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_32_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_32_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_32_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_32_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_32_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_32_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_32_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_32_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_32_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_32_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_32_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 64 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_64_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_64_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_64_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_64_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_64_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_64_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_64_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_64_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_64_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_64_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_64_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_64_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_64_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_64_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_64_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_64_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_64_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_64_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_64_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_64_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_64_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_64_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_64_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_64_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_64_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_64_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_64_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_64_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_64_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_64_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_64_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_64_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 128 */
/* ============================================================ */
/* Flops == 1 */
__global__ void intensity_kernel_128_1(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 2 */
__global__ void intensity_kernel_128_2(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 3 */
__global__ void intensity_kernel_128_3(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 4 */
__global__ void intensity_kernel_128_4(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 5 */
__global__ void intensity_kernel_128_5(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 6 */
__global__ void intensity_kernel_128_6(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 7 */
__global__ void intensity_kernel_128_7(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 8 */
__global__ void intensity_kernel_128_8(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 9 */
__global__ void intensity_kernel_128_9(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 10 */
__global__ void intensity_kernel_128_10(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 11 */
__global__ void intensity_kernel_128_11(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 12 */
__global__ void intensity_kernel_128_12(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 13 */
__global__ void intensity_kernel_128_13(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 14 */
__global__ void intensity_kernel_128_14(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 15 */
__global__ void intensity_kernel_128_15(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 16 */
__global__ void intensity_kernel_128_16(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 17 */
__global__ void intensity_kernel_128_17(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 18 */
__global__ void intensity_kernel_128_18(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 19 */
__global__ void intensity_kernel_128_19(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 20 */
__global__ void intensity_kernel_128_20(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 21 */
__global__ void intensity_kernel_128_21(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 22 */
__global__ void intensity_kernel_128_22(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 23 */
__global__ void intensity_kernel_128_23(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 24 */
__global__ void intensity_kernel_128_24(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 25 */
__global__ void intensity_kernel_128_25(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 26 */
__global__ void intensity_kernel_128_26(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 27 */
__global__ void intensity_kernel_128_27(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 28 */
__global__ void intensity_kernel_128_28(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 29 */
__global__ void intensity_kernel_128_29(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 30 */
__global__ void intensity_kernel_128_30(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 31 */
__global__ void intensity_kernel_128_31(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 32 */
__global__ void intensity_kernel_128_32(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 4 */
/* ============================================================ */
/* Flops == 64 */
__global__ void intensity_kernel_4_64(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 128 */
__global__ void intensity_kernel_4_128(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 8 */
/* ============================================================ */
/* Flops == 64 */
__global__ void intensity_kernel_8_64(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* Flops == 128 */
__global__ void intensity_kernel_8_128(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 8 */
/* ============================================================ */
/* Flops == 48 */
__global__ void intensity_kernel_8_48(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 8 */
/* ============================================================ */
/* Flops == 192 */
__global__ void intensity_kernel_8_192(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

/* ============================================================ */
/* Stream == 8 */
/* ============================================================ */
/* Flops == 160 */
__global__ void intensity_kernel_8_160(int num_threads, int num_streams, TYPE* in, TYPE* out);
/* ============================================================ */

